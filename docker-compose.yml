services:
  db:
    image: postgres:15
    container_name: postgres-db
    environment:
      POSTGRES_USER: marcelo_user
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: colegio_ohiggins
    ports:
      - "5432:5432"